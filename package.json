{
  "name": "skyflip-server",
  "version": "1.0.0",
  "description": "Skyflip flight booking app — serves the frontend and sends real itinerary emails via Brevo (free tier, no domain required).",
  "main": "index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
